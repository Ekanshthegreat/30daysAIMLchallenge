import tensorflow as tf
from tensorflow import keras #api for tensorflow to allow for less code - so we dont have to define our own tensors
import numpy as np
import matplotlib.pyplot as plt

data = keras.datasets.fashion_mnist